node_modules
dist
electron/dist
release
.env
.env.local
# Build-time env files are NOT tracked. CI writes them at release time from
# repo Variables (vars.ENV_PRODUCTION / vars.ENV_STAGING); keep local copies
# untracked for local builds.
.env.production
.env.staging
.DS_Store
*.log
# Playwright artefacts — runtime output, not source
test-results
playwright-report

# Code-signing artefacts — generated by scripts/generate-codesign-cert.sh.
# The .pfx + private key MUST NOT enter git.
codesign-out/
*.pfx
*.pfx.b64
*.key
